import React from "react";
import { AppLayout } from "@/layout";
import { DetailMemberContainer } from "@/pods";

export const DetailMemberScene: React.FC = () => {

    return (
        <AppLayout>
            <DetailMemberContainer /> 
        </AppLayout>
    );
};